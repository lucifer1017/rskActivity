{
  "name": "rsk-activity-feed",
  "private": true,
  "version": "0.1.0",
  "description": "Rsk Activity-Feed: Cross-chain history widget and bridge tracker for BTC → Rootstock.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @rootstock-kits/activity",
    "test": "npm test -w @rootstock-kits/activity",
    "lint": "npm run lint -w @rootstock-kits/activity"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}

